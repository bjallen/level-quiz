<%= form_with(model: behavior) do |form| %>
  <% if behavior.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(behavior.errors.count, "error") %> prohibited this behavior from being saved:</h2>

      <ul>
        <% behavior.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :competency_id, style: "display: block" %>
    <%= form.collection_select :competency_id, @competencies, :id, :name %>
  </div>

  <div>
    <%= form.label :level_id, style: "display: block" %>
    <%= form.collection_select :level_id, @levels, :id, :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
